const getData = async () => {
	let y = await "Hello World";  //await to wait for the promise to resolve before continuing
	console.log(y);
}

console.log(1);
getData(); // Pause for promise to filter the promise fully
console.log(2);

let  z = async (e)=>{
	let $e = await[1,2,3];
	if
	console.log($e)
}
console.log(1);
z();
